# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret = "GREgg#$3246CTf:G3gf:90Lat:{34EFewfAB`R5W:1uDFN];Ik@n"

# Disable Play from using it's own PID. Instead configure a systemd PID on the linux box.
pidfile.path = "/dev/null"

# Caches used.
play.cache.bindCaches = ["agency_cache"]

play.akka.actor-system = "commute-actor-system"

# The global application startup class
play.modules.enabled += "main.CommuteStartModule"

# Global error handler
play.http.errorHandler = "main.ErrorHandler"

# The application languages
# ~~~~~
play.i18n.langs = [ "en" ]

play.ws.ssl {
  loose.allowWeakCiphers = true
  loose.acceptAnyCertificate = true

  trustManager = {
    stores = [
//      { path = "/var/play/splendidbits/commutealerts-0.2/commuteapp.io_keystore.jks" }
      { path: ${java.home}/lib/security/cacerts } # Fallback to default JSSE trust store
      { path = "/Users/daniel/Documents/git-splendidbits/commute-alerts/commuteapp.io_keystore.jks" }
    ]
  }
}

# !!! WARNING !!
# EBeanORM Configuration
# Postgresql Database Engine Configuration
#
# If you are progromattically instanciating an EbeanServer
# with configuration ONLY uncomment the following ebean module configuration
# if you want evoluitions to create the database generation scripts, then disable.
play.ebean.defaultDatasource=commutealerts
play.evolutions.db.commutealerts.enabled=false
play.evolutions.enabled=false

# commute alerts module config.
db.commutealerts.name="commutealerts"
db.commutealerts.driver=org.postgresql.Driver
db.commutealerts.connectionTimeout=10 seconds
db.commutealerts.url="jdbc:postgresql://localhost:5432/commutealerts"
db.commutealerts.username="splendidbits"
db.commutealerts.password="kYBaf34sfd8L"
db.commutealerts.databasePlatformName="postgres"

# pushservices module config.
db.pushservices.name="pushservices"
db.pushservices.driver=org.postgresql.Driver
db.pushservices.connectionTimeout=10 seconds
db.pushservices.url="jdbc:postgresql://localhost:5432/pushservices"
db.pushservices.username="splendidbits"
db.pushservices.password="kYBaf34sfd8L"

# fluffylog module config.
db.fluffylog.url="jdbc:postgresql://localhost:5432/fluffylog"
db.fluffylog.username="splendidbits"
db.fluffylog.password="kYBaf34sfd8L"
db.fluffylog.driver=org.postgresql.Driver
db.fluffylog.client_name="commutealerts"
db.fluffylog.databasePlatformName="postgres"
db.fluffylog.timezone="America/Los_Angeles"

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your mRoute file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routesdel`.
# Default to Routes in the root package (and conf/routesdel)
# play.http.router = my.application.Routes




