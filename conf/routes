# Routes
# This file defines all application routes(Higher priority routes first)
# ~~~~

# SplendidLog
->  /logs                      splendidlog.Routes

# Home page
GET     /                      controllers.Application.index()

# Agency alerts download
#GET     /septa                 agency.SeptaAlertsController.downloadAlerts()

# Public API endpoints
POST    /gcm/v1/register              controllers.RegistrationController.register()
POST    /gcm/v1/subscribe             controllers.SubscriptionController.subscribe()

# Signup
GET     /gcm/v1/signup                controllers.SignupController.signup()
POST    /gcm/v1/signup/adduser        controllers.SignupController.addUser()

#GET     /test                  controllers.Application.personsIndex()
#POST    /person                controllers.Application.addPerson()
#GET     /persons               controllers.Application.getPersons

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file          controllers.Assets.at(path="/public", file)
