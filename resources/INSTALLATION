sudo rm -r commutealerts-0.2/

git clone -b master https://github.com/splendidbits/commute-play commutealerts-0.2
cd commutealerts-0.2
git submodule update --init --recursive
git pull --all --recurse-submodules

./activator clean compile stage dist run